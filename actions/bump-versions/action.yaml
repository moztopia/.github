name: "Bump Versions"
description: "Increment patch and append build date across multiple project files"

runs:
  using: "composite"
  steps:
    - name: Bump versions
      shell: bash
      run: |
        python3 "${{ github.action_path }}/bump_versions.py"
