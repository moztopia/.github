---
name: Markdown Validation
description: Checks Markdown files for non-empty content
runs:
  using: "composite"
  steps:
    - name: Markdown check
      shell: bash
      run: |
        python3 "${{ github.action_path }}/markdown_check.py"
