---
name: Config Validation
description: Lints YAML and JSON configuration files
runs:
  using: "composite"
  steps:
    - name: Config check
      shell: bash
      run: |
        python3 "${{ github.action_path }}/config_check.py"
